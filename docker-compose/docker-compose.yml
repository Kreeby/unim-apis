version: '3.7'
services:
  postgres:
    image: postgres:10.5
    restart: always
    environment:
      - POSTGRES_USER=rashad
      - POSTGRES_PASSWORD=cmpunk2121
    logging:
      options:
        max-size: 10m
        max-file: "3"
    ports:
      - '5438:5432'